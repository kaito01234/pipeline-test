name: Sleep test
on:
  workflow_dispatch:

jobs:
  Explore-GitHub-Actions:
    runs-on: [self-hosted, linux]
    steps:
      - run: echo "sleepします"
      - run: sleep 120
      - run: echo "sleep終了"
      
